"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = _default;

function _react() {
  const data = _interopRequireDefault(require("react"));

  _react = function _react() {
    return data;
  };

  return data;
}

function _path() {
  const data = require("path");

  _path = function _path() {
    return data;
  };

  return data;
}

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _default(api, options) {
  const cwd = api.cwd,
        compatDirname = api.compatDirname;
  api.chainWebpackConfig(webpackConfig => {
    if (options === 'preact') {
      webpackConfig.resolve.alias.set('preact/debug', require.resolve('preact/debug')).set('preact', compatDirname('preact/package.json', cwd, (0, _path().dirname)(require.resolve('preact/package.json')))).set('react', compatDirname('preact/compat/package.json', cwd, (0, _path().dirname)(require.resolve('preact/compat/package.json')))).set('react-dom/test-utils', compatDirname('preact/test-utils/package.json', cwd, (0, _path().dirname)(require.resolve('preact/test-utils/package.json')))).set('react-dom', compatDirname('preact/compat/package.json', cwd, (0, _path().dirname)(require.resolve('preact/compat/package.json')))).set('react-addons-css-transition-group', 'preact-css-transition-group');
    }
  });
  api.addEntryImport(() => {
    if (process.env.NODE_ENV === 'development' && options === 'preact') {
      return {
        source: 'preact/debug'
      };
    } else {
      return [];
    }
  });
}